package com.limpu.hita.theta.ui.topic.search

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class TopicListViewModel(application: Application) : AndroidViewModel(application)